export const basic = {
    category: 'UI组件',
    type: '通用',
    title: '状态标识组件',
    subtitle: 'WaveCircle',
}

# 水波纹

> 状态标识

## 直接在 mdx 中写
import { WaveCircle } from 'skyline-ui';

<div style={{ display: 'flex', height: 300 }}>
    <WaveCircle bgColor="pink" />
    <WaveCircle>默认</WaveCircle>
</div>

```jsx
import { WaveCircle } from 'skyline-ui';

ReactDOM.render(
    <WaveCircle bgColor="pink" />,
    mountNode,
);
```

#### 读取文件并只展示代码 ./examples/default.jsx

import CodeBlock from '@/components/code';
import DemoRaw from './examples/default?raw';

<CodeBlock code={DemoRaw} lang="jsx" />

> 或传入 path （components目录下代码所在位置） 通过 Snapshot 展示

import Snapshot from '@/components/snapshot';

## 案例1
<Snapshot path="wave-circle/examples/default" lang="jsx" />
## 案例2
<Snapshot path="wave-circle/examples/bg" lang="tsx" />

## 读取外部md文件

import MBox from '@/components/markdown';
import api from './examples/api.md?raw';

<MBox text={api} />
