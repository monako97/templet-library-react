# 水波纹

> 状态标识

import { WaveCircle } from '@components';

<div style={{ display: 'flex', height: 300 }}>
<WaveCircle bgColor="pink" />
<WaveCircle>默认</WaveCircle>
</div>

```jsx
import { WaveCircle } from '@components';

ReactDOM.render(
    <WaveCircle bgColor="pink" />,
    mountNode,
);
```

export const basic = {
    category: 'UI组件',
    type: '通用',
    title: '状态标识组件',
    subtitle: 'WaveCircle',
}
